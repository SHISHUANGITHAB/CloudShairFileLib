# 项目环境信息

> ⚠️ **两阶段说明**：本项目分两个独立阶段——**第一阶段（原型）** 和 **第二阶段（正式系统）** ，两阶段的架构、部署环境、技术栈完全不同，务必区分对待。

---

## 第一阶段：原型系统（当前阶段）

### 项目结构

```
C:\SSDATA\望城经开区\CloudShairFileLib\
├── 01_需求阶段/                   # 需求文档（两阶段共用）
│   ├── 需求功能清单（合并稿）.md    # 客户确认的功能清单
│   ├── 需求补充反馈（客户答复）.md  # Q1-Q6答复及补充说明
│   ├── 云端档案共享库-需求纲要（初步调研）.md
│   ├── 云端档案共享库.xmind        # 思维导图源文件
│   ├── index_model.html           # 参考设计
│   └── 项目档案一览表.html         # 档案一览表的参考样式
├── 02_原型系统/                   # 初始原型（v1）
│   └── index.html
├── protosystem/                   # 当前工作原型（可运行，不含备份文件）
│   ├── index.html                 # 主页面（单文件 HTML 原型，Tailwind + FontAwesome）
│   ├── js/
│   │   ├── project-archives.js    # 项目档案一览弹窗组件
│   │   └── push-dialog.js         # 推送弹窗组件
│   └── demo/
│       ├── demo.pdf / .docx / .xlsx / .png  # 示例文件
├── 99_project-memory/             # 项目记忆（本目录）
├── README.md / CLAUDE.md / .gitignore / chi_sim.traineddata
```

### 原型开发计划

| 阶段 | 状态 |
|------|------|
| 需求确认 | ✅ 已完成 |
| 原型设计 | 🔄 进行中 |

### 原型技术栈

- **纯前端**：HTML + CSS + JavaScript（单页应用）
- **UI 框架**：Tailwind CSS（CDN 引用，禁止用于正式系统）
- **图标**：FontAwesome 6.4（CDN 引用）
- **Mock 数据**：硬编码在 `index.html` 中（约 140 条档案）
- **在线预览**：KKFileView（Docker 容器，端口 8012）

---

## 第二阶段：正式系统（规划中，待启动）

### 预期源码结构（开发后增加）

```
CloudShairFileLib/
├── 第二阶段源码/                   # 正式系统源码（第一阶段完成后新建）
│   ├── credit_archive_admin/      # 后端（Java, RuoYi 框架）
│   ├── credit_archive_pc/         # PC前端（Vue 3 + Vite）
│   └── credit_archive_h5/         # H5移动端（视需求）
├── 部署文档/                       # 正式系统部署相关
└── ...
```

> ⚠️ 第一阶段原型源码（`protosystem/`）不会被第二阶段复用。正式系统从头开发。

### 预期技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite |
| 后端 | Java 8 + Spring Boot 2.5.x + RuoYi 3.9.1 |
| 在线预览 | KKFileView（开源免费，内网部署） |
| 文件存储 | 本地文件系统（待确认） |
| 数据库 | 待定（达梦 / MySQL / PostgreSQL） |
| 短信平台 | 已有接口文档，对接园区现有短信平台 |
| 部署 | Docker 容器化部署 |

> ⚠️ 正式系统的服务器、网络架构、域名等均与原型系统不同，**不能复用原型演示服务器的部署配置**。

---

## 服务器环境（两阶段严格分离）

### 🔵 第一阶段：原型演示服务器

| 项目 | 值 |
|------|-----|
| **用途** | 仅用于原型展示，不算正式系统 |
| 地址 | 183.215.41.66:8028 |
| 用户 | wongxin |
| 操作系统 | Ubuntu 22.04 LTS (x86_64) |
| Docker | 29.3.1 |
| 原型端口 | **9598**（nginx 容器） |
| KKFileView 端口 | **8012**（Docker 容器） |
| 存储空间 | 785G / 545G 可用 |
| 内存 | 31G / 25G 可用 |

### 🟢 第二阶段：正式系统服务器（待定）

| 项目 | 值 |
|------|-----|
| **用途** | 正式生产环境 |
| 地址 | **待定**（与原型服务器不同） |
| SSH 端口 | **待定** |
| 用户 | **待定** |
| 操作系统 | **待定** |
| 架构 | **待定**（可能为 aarch64，参考信用数智系统） |
| 域名 | **待定**（需配置 HTTPS） |
| 端口规划 | **待定**（与原型 9598/8012 无关） |
| 数据库 | **待定**（达梦可能性大） |
| 部署方式 | **待定**（参考信用数智系统的 Docker 部署模式） |

> ⚠️ 正式系统部署前会重新确定服务器信息，届时更新本文件。
